# Curling Shot Type Prediction

First download the dataset. Read the file in the dataset folder to learn how
to do that.

Then you can run `python main.py` to train and evaluate a single instance of
our final model.

If you want to see how to enable data augmentation, different regularization,
or perform hyperparameter search, search for `TA_HELP` in `main.py`.